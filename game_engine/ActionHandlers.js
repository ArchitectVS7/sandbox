// Placeholder action handlers
const actions = {
  move(state, params) {
    // TODO: implement move logic
    return { state, message: 'You move somewhere.' };
  },
  inspect(state, params) {
    // TODO: implement inspect logic
    return { state, message: 'There is nothing special.' };
  }
};

module.exports = actions;
